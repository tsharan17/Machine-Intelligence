#include <Arduino.h>

void setup() {
    pinMode(32, OUTPUT);
    pinMode(33, OUTPUT);
    pinMode(2, OUTPUT);
    pinMode(4, OUTPUT);
    pinMode(5, OUTPUT);
    pinMode(18, OUTPUT);
    pinMode(19, OUTPUT);
}

void loop() {
    while(true);
}